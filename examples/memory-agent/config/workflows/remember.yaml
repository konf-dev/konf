workflow: remember
description: "Store a memory in the knowledge graph"
register_as_tool: true
capabilities: ["memory:store"]
nodes:
  store:
    do: memory:store
    with:
      nodes:
        - content: "A memory stored via Konf workflow"
          node_type: "memory"
          metadata:
            source: "konf-example"
    return: true
