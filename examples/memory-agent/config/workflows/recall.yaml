workflow: recall
description: "Search memories"
register_as_tool: true
capabilities: ["memory_search"]
nodes:
  search:
    do: memory_search
    input:
      query: "{{ message }}"
      limit: 5
    return: true
