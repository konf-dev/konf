workflow: chat
description: "Store the user's message as a memory"
capabilities: ["memory_store"]
nodes:
  store:
    do: memory_store
    with:
      nodes:
        - content: "User said something"
          node_type: "memory"
          metadata:
            source: "konf-memory-agent-example"
    return: true
