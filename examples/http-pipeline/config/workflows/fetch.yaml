workflow: fetch_data
description: "Fetch data from an API and return it"
register_as_tool: true
capabilities: ["http_get"]
nodes:
  fetch:
    do: http_get
    with:
      url: "https://httpbin.org/json"
    return: true
