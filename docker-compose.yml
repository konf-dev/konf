services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      KONF_CONFIG_DIR: /config
      KONF__DATABASE__URL: postgresql://postgres:${POSTGRES_PASSWORD:-konf}@postgres/konf
      KONF__AUTH__SUPABASE_URL: http://localhost:9999
      KONF__MCP_ENABLED: "true"
    volumes:
      - ./products/devkit/config:/config:ro
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/v1/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3

  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-konf}
      POSTGRES_DB: konf
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
