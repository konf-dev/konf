name: assistant
description: "Personal assistant with memory and tool use"
version: "0.1.0"

triggers:
  chat:
    workflow: chat
    capabilities:
      - "memory_*"
      - "ai_complete"
      - "http_get"
